<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8"/>
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>"/>
        <link   rel="stylesheet" href="{{asset('css/bootstrap.min.css')}}" type='text/css'/>
        
        {% block stylesheets %}
            
        {% endblock %}
               {% block javascripts %}
            <script  src='{{asset("js/search.js")}}' type="text/javascript"></script>
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-kQtW33rZJAHjgefvhyyzcGF3C5TFyBQBA13V1RKPf4uH+bwyzQxZ6CmMZHmNBEfJ" crossorigin="anonymous" type="text/javascript"></script>

        {% endblock %}
        {% block javascripts2   %}
        {% endblock %}
        
    </head>
    <body class="container-fluid">
      <div class="m-5">
         <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
          <div class="container-fluid">
            <a class="navbar-brand" href="{{path('home')}}">N shop </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
              <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                  <a class="nav-link active" aria-current="page" href="{{path('home')}}">Home</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="{{path('product')}}">Produit</a>
                </li>
                 {%  if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item">
                  <a class="nav-link" href="{{path('profile')}}">Profile</a>
                </li>
                  {% endif %}
                
                  <li class="nav-item">
                  <a class="nav-link" href="{{path('cart')}}">Panier</a>
                </li>
               
              </ul>
              <div style="margin: auto ; margin-right: 0px;" class="d-flex ">
                <div class="form-inline my-2 my-lg-0">
                  <input class="form-control  mr-sm-2" list="datalistOptions" id="exampleDataList" placeholder="Type to search...">
                  <datalist id="datalistOptions">
                  </datalist>
                </div>
                {# affiche conditionnel si utilisateur est connecter #}
                {%  if is_granted('IS_AUTHENTICATED_FULLY') %}
                  <a href='{{path("cart")}}' class=" ml-2 mr-2 btn btn-primary">  Aller  au panier</a>
                  <a href='{{path("app_logout")}} ' class="btn btn-primary"> Deconnection</a>
                {% else %}
                  <a href='{{path("login")}}' class=" ml-2 mr-2 btn btn-primary"> Connection</a>
                  <a href='{{path("app_register")}}' class="btn btn-primary"> S'inscrire</a>
                {% endif %}
              </div>
            </div>
          </div> 

        </nav>
      <div>
        {% block body %}{% endblock %}
    </body>

</html>
