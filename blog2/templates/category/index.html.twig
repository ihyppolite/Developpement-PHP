{% extends 'base.html.twig' %}

{% block title %}Hello CategoryController!{% endblock %}

{% block body %}


<div class="example-wrapper">
    <a href="{{ path('this_category', {id:category.id}) }}" > {{category.name}}</a>

    {{category.description}}
</div>
{% endblock %}
